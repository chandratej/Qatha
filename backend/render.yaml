# Render Blueprint — Katha API (free-tier beta fallback when Railway trial/plan blocks)
# Dashboard: New → Blueprint → this file, or Web Service with root backend/

services:
  - type: web
    name: katha-api
    runtime: node
    plan: free
    # If this file is used as service root = backend, omit rootDir or leave empty.
    buildCommand: npm ci --omit=dev
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: MOCK_MODE
        value: "false"
      - key: CREATOR_SHARE_PCT
        value: "40"
      - key: LAUNCH_OFFER_MODE
        value: immediate
      - key: KATHA_UNPROVEN_FREE_CHAPTERS
        value: "12"
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_PUBLISHABLE_KEY
        sync: false
      - key: SUPABASE_SECRET_KEY
        sync: false
      - key: RAZORPAY_KEY_ID
        sync: false
      - key: RAZORPAY_KEY_SECRET
        sync: false
      - key: RAZORPAY_WEBHOOK_SECRET
        sync: false
      - key: SPI_BATCH_SECRET
        generateValue: true
      - key: OPENAI_API_KEY
        sync: false
      - key: ALLOWED_ORIGINS
        value: http://localhost:5173,http://127.0.0.1:5173,https://studio.katha.in,https://katha.app
